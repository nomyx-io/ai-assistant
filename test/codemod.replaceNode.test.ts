import fs from 'fs';
import ts from 'typescript';
jest.mock('fs');

describe('replaceNode function tests', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  // Test cases will be added here
});

