import fs from 'fs';
import ts from 'typescript';
jest.mock('fs');

describe('removeNode function tests', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  // Test cases will be added here
});

